# Kepware Monitor OPC - Python Dependencies
#
# 版本皆加上「下一個大版本」上限，避免正式機以離線手動 pip install 時
# 抓到不相容的新版（開發/驗證機與正式機安裝時間不同，容易裝到不同版本）。
# asyncua 特別重要：1.x 與 2.x 的 Client API 不同（is_alive() 已針對此做
# 相容處理），正式機實測為 2.0.x，故鎖在 2.x 系列，不可降到 1.x。

# OPC UA 連線（正式環境實測 asyncua 2.0.1）
asyncua>=2.0.0,<3.0.0

# Webhook 推播
requests>=2.28.0,<3.0.0

# Web UI（選用，WebUI.Enable = true 時需要）
fastapi>=0.100.0,<1.0.0
uvicorn>=0.20.0,<1.0.0
jinja2>=3.1.0,<4.0.0
python-multipart>=0.0.5,<1.0.0
